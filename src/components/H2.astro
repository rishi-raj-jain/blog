---
interface Props {
  class?: string;
  transitionName?: string;
}

const { transitionName, class: className } = Astro.props;
---

<h2 transition:name={transitionName} class={`text-2xl mb-4 ${className}`}>
  <slot />
</h2>
